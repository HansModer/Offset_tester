from setuptools import setup, find_packages

setup(
    name='offset-tester',
    version='1.0.0',
    description='A library to test offsets in buffers and memory safely.',
    author='Your Name',
    packages=find_packages(),
    install_requires=[],
    classifiers=[
        'Programming Language :: Python :: 3',
        'License :: OSI Approved :: MIT License',
    ],
)
